{
  "name": "apinews",
  "version": "1.0.0",
  "description": "API de noticias con JSON Server",
  "scripts": {
    "start": "json-server --watch db.json --port 3000 --routes routes.json"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
